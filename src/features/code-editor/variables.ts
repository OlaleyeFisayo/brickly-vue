import type {
  EditorStates,
} from "./types";

export const EDITOR_STATES: EditorStates = {
  tabSize: 2,
  indentWithTab: true,
  style: {
    width: "600px",
    borderRight: "1px var(--primary-700)",
    color: "white",
  },
};
